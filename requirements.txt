# KURE 번아웃 감정 분류 모델 + AI 서버 의존성
# Python 3.10+ 권장

# ============================================
# 핵심 의존성
# ============================================

# PyTorch — CUDA 버전은 아래 명령으로 별도 설치 (requirements.txt로 설치 시 CPU 버전 설치됨)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# torch>=2.0.0  # 주석 처리: pip install -r 사용 시 PyPI CPU 버전이 덮어쓸 수 있음

# Transformers & Sentence Transformers
transformers>=4.34.0
sentence-transformers>=2.2.0

# ============================================
# FastAPI 서버
# ============================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0          # 비동기 HTTP 클라이언트 (콜백용)
python-multipart>=0.0.6  # 파일 업로드

# ============================================
# 데이터 처리
# ============================================
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
orjson>=3.9.0          # JSON 처리 (대용량)
pydantic>=2.0.0        # 데이터 검증

# ============================================
# 유틸리티
# ============================================
tqdm>=4.65.0           # 진행률 표시
python-dotenv>=1.0.0   # 환경변수 관리

# ============================================
# (선택) LLM 피드백 - GPU 메모리 필요
# ============================================
# accelerate>=0.24.0
# bitsandbytes>=0.41.0

# ============================================
# (선택) 개발/테스트
# ============================================
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
